import { defineConfig } from 'oxfmt';

export default defineConfig({
  printWidth: 80,
  trailingComma: 'es5',
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  sortPackageJson: false,
  ignorePatterns: [],
});
