import { createNamespace } from 'continuation-local-storage';

// TODO set logging namespace e.g. fi.rakennustieto.hot
const ns = createNamespace('TODO.TODO.server-template');

export default ns;
export { ns as ns, ns as namespace };
