# Optional stack components

## Shared

The `shared` directory is available for all Docker containers. You can use it for shared code, such as linked npm libraries used by both the client and server.
