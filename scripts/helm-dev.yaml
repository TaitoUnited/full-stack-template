# Overrides for dev environment

full-stack:
  services:
    server:
      env:
        # Uncomment to enable REST API docs and GraphQL playground on dev
        # API_DOCS_ENABLED: true
        # API_PLAYGROUND_ENABLED: true