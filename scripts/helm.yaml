stack:
  admin: admin
  bot:
  cache:
  client: client
  database: database
  queue:
  server: server
  storage: storage
  worker:

# Common settings

logLevel: info    # trace, debug, info, warn, error, fatal
debug: 'false'
repository: ${taito_registry}
imagePullPolicy: ''

domains:
  - ${taito_domain}

# Stack settings

ingress:
  basicAuth: true
  class: nginx
  maxBodySize: 1m

admin:
  replicas: ${kubectl_replicas}
  env:
    SENTRY_PUBLIC_DSN: #sentryPublicDSN

bot:
  replicas: ${kubectl_replicas}
  secrets:
    ADMIN_PASSWORD: ${taito_project}-${taito_env}-admin.password
    USER_PASSWORD: ${taito_project}-${taito_env}-user.password
  env:
    SENTRY_DSN: #sentryDSN
    DEBUG: false
    ENV: production

client:
  replicas: ${kubectl_replicas}
  env:
    SENTRY_PUBLIC_DSN: #sentryPublicDSN

server:
  replicas: ${kubectl_replicas}
  secrets:
    JWT_SECRET: ${taito_project}-${taito_env}-jwt.secret
    ADMIN_PASSWORD: ${taito_project}-${taito_env}-admin.password
    USER_PASSWORD: ${taito_project}-${taito_env}-user.password
    S3_KEY_SECRET: ${taito_project}-${taito_env}-storage-gateway.secret
  env:
    SENTRY_DSN: #sentryDSN
    DEBUG: false
    # TODO remove ENV or set it automatically?
    ENV: production
    S3_URL: http://${taito_project}-storage:9000/
    S3_REGION: ${taito_provider_region}
    S3_BUCKET: ${taito_project}-${taito_env}
    S3_KEY_ID: 070UEOQR6LX4YPZLFU0V

worker:
  replicas: ${kubectl_replicas}
  secrets:
    S3_KEY_SECRET: ${taito_project}-${taito_env}-storage-gateway.secret
  env:
    SENTRY_DSN: #sentryDSN
    DEBUG: false
    ENV: production
    S3_URL: http://${taito_project}-storage:9000/
    S3_REGION: ${taito_provider_region}
    S3_BUCKET: ${taito_project}-${taito_env}
    S3_KEY_ID: 070UEOQR6LX4YPZLFU0V

database:
  port: 5432
  db: ${taito_zone}:${taito_provider_region}:${db_database_instance}
  poolMin: 5
  poolMax: 10

# minio as storage gateway
storage:
  image: minio/minio
  replicas: ${kubectl_replicas}
  host: storage
  region: ${taito_provider_region}
  secrets:
    MINIO_SECRET_KEY: ${taito_project}-${taito_env}-storage-gateway.secret
  env:
    MINIO_ACCESS_KEY: 070UEOQR6LX4YPZLFU0V

cache:
  image: redis:alpine
  replicas: 1 # TODO multiple replicas as redis cluster

queue:
  image: rabbitmq:alpine
  replicas: 1 # TODO multiple replicas as queue cluster

# TODO configure resources
# resources: {}
  #  cpu: 100m
  #  memory: 128Mi
  #requests:
  #  cpu: 100m
  #  memory: 128Mi
